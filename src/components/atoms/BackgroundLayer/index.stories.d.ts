import type { Meta, StoryObj } from '@storybook/react';
import { BackgroundLayer } from '.';
declare const meta: Meta<typeof BackgroundLayer>;
export default meta;
type Story = StoryObj<typeof BackgroundLayer>;
export declare const Default: Story;
